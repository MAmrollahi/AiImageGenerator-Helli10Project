// Example JavaScript for a smooth scroll to the input page
const btn = document.querySelector('.get-started-btn');
btn.addEventListener('click', () => {
    window.scrollTo({
        top: 0,
        behavior: 'smooth'
    });
    // Replace this with code to redirect to the input page (generate-qr.html)
});
